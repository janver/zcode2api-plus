node_modules/
js-code/node_modules/
# 管理後台 SPA（frontend/ 的依賴；dist 由 go:embed 引用，必須入庫）
frontend/node_modules/
# Python
__pycache__/
*.py[cod]
.venv/
venv/
# 运行时数据（账号 SQLite 等），绝不可入库
data/
data_test/
# Compose bind mount 的运行时数据（含账号 SQLite、Chromium 缓存）
zcode-data/
zcode-browser/
# 账号导出（含明文凭证；export 子命令默认写到这里）
zcode-accounts.json

.env
js-code/

# 本机部署与反查临时文件
/temp/
.tmp/
/nginx.conf
.pytest_cache/
